# Generators Range 

x = [1,2,3]
'''We can actually use the generators for large set of numbers instead of storing into its memory'''
for x in range(0,10,2): # here range (0,10,2) is (start, stop, break)
    print(x)